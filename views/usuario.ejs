<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservas</title>
    <link rel="stylesheet" type="text/css" href="/style.css">
</head>
<body>
    <header>
        <h1>¡Gestión de usuarios por <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Lista de usuarios</h2>
            <ul class="product-list">
                <% data.usuaris.forEach(usuario => { %>
                    <li class="product-item">
                        <strong><%= usuario.correo %></strong> 
                        <span><%= usuario.contrasenya %></span>
                        <span><%= usuario.tipo %></span>
                        <span><a href="/usuarios/<%=usuario.id%>" class="botonDetalle">Detalle</a></span>
                    </li>
                <% }) %>
            </ul>
        </section>
    </main>
</body>
</html>